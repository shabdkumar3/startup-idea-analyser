name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '0 */10 * * *'  # Every 10 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://ai-stock-research-assistant.streamlit.app/ > /dev/null